# Solana RPC used by the wallet adapter (defaults to public mainnet-beta)
NEXT_PUBLIC_SOLANA_RPC_URL=
# Bearer token required by /api/cron/prices when set
CRON_SECRET=
# Anchor prize vault program id; claiming stays disabled until this is set
NEXT_PUBLIC_PRIZE_VAULT_PROGRAM_ID=
